export async function extractTextFromImage(base64: string): Promise<string> {
  // Placeholder: Replace with actual OCR logic
  return new Promise((resolve) => setTimeout(() => resolve('Extracted text from image!'), 2000));
} 